server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/sunnyvet_micro
    username: root
    password: rootpassword
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true